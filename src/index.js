import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'


//this is the main page, we will render each page upon selection
//we need a Navigation component

ReactDOM.render(
    <App />, document.getElementById('root')
);
